[phases.setup]
nixPkgs = ["bun", "curl", "bash"]

[phases.install]
cmds = [
  "bun install --frozen-lockfile",
  "curl -sSL https://raw.githubusercontent.com/okx/onchainos-skills/main/install.sh | sh",
  "cp /root/.local/bin/onchainos /usr/bin/onchainos"
]

[start]
cmd = "bun run src/index.ts"
